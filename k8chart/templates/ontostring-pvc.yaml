apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ontotools-curation-logs
  namespace: ontotools
spec:
  storageClassName: local-storage
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi

